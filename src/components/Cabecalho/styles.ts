import styled from 'styled-components'
import { corPrincipal, corSecundaria } from '../../styles'

export const SCabecalho = styled.header`
  background-color: ${corSecundaria};
  color: ${corPrincipal};
  text-align: center;
  padding: 24px 0;
`
